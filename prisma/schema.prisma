// Minimal schema for issue #116 (DB connectivity). Core models come in #117.
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
