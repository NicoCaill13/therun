/**
 * Central Prisma re-export (.cursorrules). Application code imports from here, not from deep generated paths.
 */
export { PrismaClient, Prisma } from '@prisma/client';
export type { SpontaneousRun } from '@prisma/client';
